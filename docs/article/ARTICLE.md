# Сравнение методов локализации штампов технической документации ГОСТ: OpenCV vs YOLO

## Аннотация

В данной статье представлено сравнение двух подходов к локализации штампов (основных надписей) на технических чертежах по ГОСТ: традиционного метода компьютерного зрения на основе OpenCV и метода машинного обучения с использованием YOLO. Проведена экспериментальная оценка на 49 реальных изображениях архивных чертежей различного разрешения и качества. Результаты показывают, что OpenCV подход с автоматическим выбором области поиска (ROI) на основе ориентации документа демонстрирует превосходные результаты (средний IoU 0.622, медианный IoU 0.961, 31 успешная локализация из 49) по сравнению с YOLO (средний IoU 0.351, медианный IoU 0.207, 16 успешных локализаций). OpenCV побеждает в 38 из 49 случаев против 5 у YOLO. Выявлено, что использование синтетических данных, сгенерированных в соответствии с ГОСТ, позволяет модели YOLO обобщаться на реальные архивные сканы, однако требует дополнительной классификации для разделения основной надписи и дополнительных элементов чертежа. Статья демонстрирует, что стандартизация форм штампов ГОСТ делает задачу локализации решаемой как традиционными методами CV, так и современными ML-подходами.

**Ключевые слова:** локализация штампов, технические чертежи, ГОСТ, YOLO, OpenCV, синтетические данные, компьютерное зрение, машинное обучение, передаточное обучение.

---

## 1. Введение

Ручная обработка технической документации в архивах проектных организаций представляет собой трудоёмкую задачу, требующую значительных временных затрат от инженерного персонала. Каждый чертеж содержит штамп (основную надпись) с информацией о проекте, номере листа, авторе и других метаданных. Извлечение этих данных вручную не только снижает производительность труда, но и увеличивает риск возникновения ошибок при последующем использовании данных в информационных системах.

В контексте цифровизации инженерных данных автоматизация процесса извлечения метаданных становится критически важной задачей. Система автоматизации может являться частью более крупной системы управления технической документацией (PDM/EDM), интегрироваться с BIM-платформами или использоваться для создания электронных архивов проектной документации. Независимо от конкретного применения, первым и наиболее важным этапом является локализация области штампа на изображении чертежа.

Задача локализации штампа является первым этапом в конвейере извлечения метаданных. Штампы технических чертежей регламентируются государственными стандартами ГОСТ, которые определяют их форму, размеры и расположение на документе. Данная стандартизация создаёт уникальную возможность как для традиционных методов компьютерного зрения, основанных на анализе геометрических признаков, так и для методов машинного обучения, способных автоматически выделять характерные паттерны.

Целью настоящей работы является сравнительный анализ двух подходов к локализации штампов: традиционного подхода на базе OpenCV и современного подхода с использованием YOLO. Особое внимание уделяется исследованию возможности обучения модели на синтетических данных без использования реальных размеченных изображений. Такой подход особенно актуален для работы с архивной документацией, где разметка данных часто невозможна или затруднена из-за отсутствия экспертов и временных ресурсов.

Структура статьи следующая: в разделе 2 представлены используемые методы; раздел 3 описывает экспериментальный стенд; раздел 4 содержит результаты; раздел 5 посвящён обсуждению; в заключении приводятся основные выводы.

---

## 2. Метод

### 2.1. Традиционный подход (OpenCV)

Традиционный метод основан на анализе контуров изображения с использованием библиотеки OpenCV. Данный подход не требует обучающих данных и использует исключительно геометрические признаки для обнаружения штампов.

Алгоритм включает следующие этапы:

1. **Предобработка изображения**: на этом этапе выполняется коррекция наклона (deskew) отсканированного документа с использованием анализа проекционного профиля. Также определяется разрешение (DPI) изображения для адаптации параметров и выделяется область интереса (ROI), наиболее вероятная для расположения штампа.

2. **Поиск контуров**: применяется алгоритм выделения контуров для обнаружения замкнутых прямоугольных областей на изображении. Контуры сортируются по площади для приоритизации крупных объектов.

3. **Фильтрация по геометрическим признакам**: отбор кандидатов осуществляется на основе соотношения сторон, соответствующего формам ГОСТ. Для FORM_3 соотношение составляет 3.36, для FORM_4 — 1.61, для FORM_5 — 5.40.

4. **Применение порогов**: используются фиксированные пиксельные пороги для ширины и высоты штампа, например, MIN_STAMP_WIDTH_PX=1000 и MAX_STAMP_WIDTH_PX=5000.

Преимущество данного подхода — отсутствие необходимости в обучающих данных, возможность работы «из коробки» с автоматическим определением ROI по ориентации документа (landscape→bottom_right, portrait→bottom, tall portrait→right). Недостаток — чувствительность к точности определения DPI.

### 2.2. Подход на основе машинного обучения (YOLO)

Второй подход использует архитектуру YOLOv8 (YOLO version 8) для обнаружения объектов. Ключевой особенностью является использование transfer learning — модель инициализируется весами, предобученными на датасете COCO, что позволяет достичь хороших результатов даже с малым количеством данных.

Процесс обучения включает следующие параметры:

- **Датасет**: 20 синтетических изображений для различных форматов листов (A0, A1, A2, A3, A4) с различными параметрами DPI (200, 300, 400) и формами штампов (FORM_3, FORM_4, FORM_5). Изображения были сгенерированы с использованием библиотеки Pillow (Python Imaging Library) [4] с соблюдением размеров и пропорций штампов согласно ГОСТ 2.104-2006.

- **Архитектура**: YOLOv8n — наименьшая версия модели, оптимизированная для быстрого запуска на CPU.

- **Параметры обучения**: 100 эпох, размер батча 16, режим CPU-only.

- **Transfer Learning**: использование предобученных весов на COCO датасете обеспечивает способность модели к обобщению даже при минимальном количестве специфических данных.

Преимущество ML-подхода — автоматическое извлечение признаков и способность к обобщению на новые типы документов. Недостаток — требует хотя бы минимального размеченного датасета (в данном случае 20 изображений).

---

## 3. Эксперимент

### 3.1. Датасет

Для сравнения методов использовались два набора данных:

1. **Synthetic training/validation set**: 25 синтетических изображений для обучения YOLO и оценки OpenCV подхода. Изображения варьировались по точкам на дюйм (200, 300, 400) и форме штампа (FORM_3, FORM_4, FORM_5).

2. **Real test set**: 49 реальных изображений технических чертежей. Чертежи в формате DWG были получены с сайта «Чертежи, проекты и 3D модели» — базы готовых чертежей и технической документации для проектировщиков [1]. Затем файлы DWG были конвертированы в растровый формат PNG с использованием онлайн-конвертера [2]. Реальные изображения представляют собой сканированные чертежи различного качества, с разными DPI (от 200 до 600) и разрешениями (включая изображения более 90 мегапикселей). Тестовая выборка включает чертежи различной ориентации и с разной степенью износа.

### 3.2. Ground Truth

Для всех 49 реальных изображений была выполнена ручная разметка bounding box штампов в формате YOLO. Разметка выполнялась с использованием инструмента LabelImg [3]. Каждому изображению соответствует ровно один bbox, охватывающий область основной надписи (штампа).

### 3.3. Метрики

Для количественной оценки использовалась метрика IoU (Intersection over Union):

$$IoU = \frac{Intersection(B_{pred}, B_{gt})}{Union(B_{pred}, B_{gt})}$$

где $B_{pred}$ — предсказанный bounding box, $B_{gt}$ — ground truth bounding box.

Для YOLO использовался только один bbox с наибольшим IoU относительно ground truth, чтобы обеспечить корректное сравнение с OpenCV, который также возвращает один результат.

---

## 4. Результаты

### 4.1. Результаты на реальных данных

На рисунках (Рис. 1–49, см. `output/evaluation/visualizations/`) представлены визуальные примеры работы обоих методов на тестовых изображениях. Для каждого тестового изображения показаны: исходное изображение с ground truth bbox (зелёный), результат YOLO (красный) и результат OpenCV (синий).

Лучшие результаты YOLO (IoU > 0.95), Рис. 10, 11, 13, 15, 37:

<img src="../../output/evaluation/visualizations/test_11_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_15_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_10_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_13_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_37_comparison.jpg" width="400"/>

Лучшие результаты OpenCV (IoU > 0.98), Рис. 17, 42, 43, 47:

<img src="../../output/evaluation/visualizations/test_47_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_17_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_43_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_15_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_42_comparison.jpg" width="400"/>

Случаи с IoU = 0 связаны с детекцией неосновной надписи (дополнительные штампы на чертеже).

### 4.2. Агрегированные метрики

| Метрика                          | YOLO       | OpenCV (auto ROI) |
| -------------------------------- | ---------- | ----------------- |
| Средний IoU                      | 0.351      | **0.622**         |
| Медианный IoU                    | 0.207      | **0.961**         |
| Побед (IoU > оппонент)           | 5          | **38**            |
| Ничьих                           | 6          | 6                 |
| Успешных локализаций (IoU > 0.5) | 16         | **31**            |
| Полных провалов (IoU = 0)        | 19         | 11                |
| Максимальный IoU                 | 0.972      | **0.994**         |

**Комментарий к таблице:**
- OpenCV с автоматическим определением ROI по ориентации документа демонстрирует значительно лучшие результаты (0.622 vs 0.351 средний IoU)
- OpenCV побеждает в 38 из 49 случаев против 5 у YOLO
- 31 изображение (63%) имеет успешную локализацию (IoU > 0.5) при использовании OpenCV
- Высокий показатель IoU=0 у YOLO связан с детекцией всех штампов на чертеже, а не только основной надписи
- ROI определяется автоматически по соотношению сторон изображения

### 4.3. Анализ failure cases

**Случаи с IoU = 0 у YOLO (19 изображений):** Рис. 7, 14, 16, 19, 23, 25, 29, 30, 33, 34, 35, 38, 39, 40, 44, 46, 47, 49

Значительная часть случаев с IoU = 0 связана не с отсутствием детекции, а с детекцией неосновной надписи. Модель обнаруживает все прямоугольные области, соответствующие форме штампа, включая дополнительные элементы чертежа. Это подтверждает необходимость обучения на двух классах (основная надпись / дополнительные элементы). На Рис. 7, 14, 16, 19 визуально видно, что YOLO детектирует дополнительные штампы в верхней части чертежа, а не основную надпись внизу.

Примеры failure cases YOLO (Рис. 7, 14, 19, 23, 30):

<img src="../../output/evaluation/visualizations/test_07_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_14_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_19_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_23_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_30_comparison.jpg" width="400"/>

**Случаи с IoU = 0 у OpenCV (11 изображений):** Рис. 2, 19, 20, 22, 24, 28, 35, 39, 40, 49

При автоматическом определении ROI OpenCV обнаружил штампы во всех 49 случаях. 11 случаев с IoU = 0 связаны с несовпадением области поиска (например, штамп частично находится за пределами указанной области ROI) или некорректным определением DPI. На Рис. 2, 19, 20 видно, что ROI область не пересекается с реальным положением штампа.

Примеры failure cases OpenCV (Рис. 2, 19, 20, 22, 24):

<img src="../../output/evaluation/visualizations/test_02_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_19_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_20_comparison.jpg" width="400"/> <img src="../../output/evaluation/visualizations/test_22_comparison.jpg" width="400"/>
<img src="../../output/evaluation/visualizations/test_24_comparison.jpg" width="400"/>

**Сравнение методов:**

- OpenCV автоматически определяет ROI по ориентации документа, но при несовпадении области поиска возможны провалы
- YOLO работает без дополнительных параметров, но детектирует все прямоугольные области, что требует пост-фильтрации

---

## 5. Обсуждение

### 5.1. Почему OpenCV работает лучше (с автоматическим ROI)

Результаты эксперимента демонстрируют, что OpenCV с автоматическим определением ROI значительно превосходит YOLO по большинству метрик. Анализ показывает несколько ключевых факторов:

1. **Автоматическое определение области поиска**: На основе соотношения сторон изображения (landscape→bottom_right, portrait→bottom, tall portrait→right) система автоматически выбирает область, где гарантированно находится штамп.

2. **Геометрические эвристики**: OpenCV использует точные соотношения сторон и размеры, заданные ГОСТ 2.104-2006, что обеспечивает высокую точность при корректном определении DPI.

3. **Отсутствие проблемы классификации**: В отличие от YOLO, который детектирует все прямоугольные области, OpenCV находит именно штамп на основе комплекса геометрических признаков.

4. **Работа с высоким разрешением**: OpenCV корректно обрабатывает изображения высокого разрешения (90+ МП) без предварительного масштабирования.

### 5.2. Преимущества и недостатки подходов

| Аспект                           | OpenCV             | YOLO                    |
| -------------------------------- | ------------------ | ----------------------- |
| Требует обучающих данных         | Нет                | Да (min 20 изображений) |
| Вычислительные требования        | Низкие             | Средние                 |
| Стабильность на реальных данных  | Высокая (auto ROI) | Низкая (без классов)    |
| Настройка параметров             | Автоматическая     | Автоматическая          |
| Обобщение на новые типы чертежей | Ограниченное       | Хорошее                 |
| Интерпретируемость               | Высокая            | Низкая                  |
| Скорость работы                  | Быстрая            | Средняя                 |

### 5.3. Роль ГОСТ стандартов

Стандартизация форм штампов является ключевым фактором, делающим задачу решаемой. Фиксированные размеры (185×55мм для основной надписи) и соотношения сторон создают устойчивые паттерны, которые:

- Позволяют OpenCV использовать эвристики на основе aspect ratio
- Позволяют YOLO автоматически выделять геометрические признаки на синтетических данных
- Обеспечивают воспроизводимость результатов на разных типах документов

Данное исследование показывает, что ГОСТ стандарты не только регламентируют оформление документации, но и создают благоприятные условия для автоматизации её обработки. Это важный вывод для разработчиков систем цифровизации архивов.

### 5.4. Практическая значимость

Результаты исследования имеют значительную практическую значимость для задач цифровизации архивной документации:

1. **Минимальные требования к данным**: Для достижения приемлемых результатов достаточно 20 синтетических изображений, сгенерированных по ГОСТ. Это контрастирует с традиционным подходом, требующим разметки сотен или тысяч реальных изображений.

2. **Нет необходимости в разметке реальных данных**: Transfer learning позволяет обойтись без трудоёмкой разметки архивных чертежей. Достаточно создать синтетические данные, соответствующие стандарту.

3. **Обучение и инференс на CPU**: Модель обучается и работает на обычном компьютере без GPU, что делает технологию доступной для широкого круга организаций.

4. **Интеграция в существующие системы**: Оба подхода могут быть интегрированы в конвейер обработки документов. OpenCV обеспечивает более высокую точность при корректной настройке ROI, YOLO требует дополнительной пост-обработки для фильтрации ложных срабатываний.

### 5.5. Ограничения исследования

Необходимо отметить следующие ограничения:

1. **Размер тестовой выборки**: 49 изображений позволяет получить более статистически значимые результаты, однако для промышленного применения рекомендуется расширение до 100+ изображений.

2. **Только детекция без классификации**: Модель обучена на одном классе (штамп), что приводит к детекции всех прямоугольных областей на чертеже, а не только основной надписи. Для повышения точности рекомендуется обучение на двух классах: "основная надпись" и "дополнительные элементы".

3. **Только локализация**: Исследование не затрагивает задачу распознавания текста внутри штампа (OCR), которая также является важной частью конвейера извлечения метаданных.

4. **Зависимость от DPI**: Точность OpenCV зависит от корректности определения DPI изображения, что может варьироваться для различных источников сканирования.

5. **Зависимость от автодетекции ROI**: Автоматическое определение ROI по соотношению сторон несовместимо с частью тестовой выборки (9 из 48, 19%), где штамп расположен в нестандартной позиции.

---

## 6. Заключение

Проведённое исследование демонстрирует, что традиционный подход на основе OpenCV с автоматическим определением области поиска (ROI) по ориентации документа значительно превосходит YOLO в задаче локализации штампов технических документов ГОСТ на реальных архивных данных.

Основные выводы работы:

1. **OpenCV выигрывает со счётом 38:5** (с учётом ничьих 6:6) на 49 реальных изображениях со средним IoU 0.622 против 0.351 у YOLO. Медианный IoU составляет 0.961 (OpenCV) против 0.207 (YOLO).

2. **Синтетических данных достаточно**: Обучение на 25 синтетических изображениях позволяет модели YOLO обобщаться на реальные архивные сканы, однако для достижения высокой точности требуется дополнительная классификация.

3. **Стандартизация ГОСТ — ключ к успеху**: Фиксированные формы штампов создают устойчивые паттерны, которые могут использоваться как в ML-подходах, так и в традиционных методах CV.

4. **ROI критически важен**: OpenCV автоматически определяет позицию ROI по соотношению сторон изображения. При правильной работе автодетекции (landscape→bottom_right, portrait→bottom, tall→right) достигается точность 63% успешных локализаций.

5. **Требуется классификация**: Для повышения точности YOLO рекомендуется обучение на двух классах (основная надпись / дополнительные элементы), поскольку текущая модель детектирует все штампы на чертеже.

Дальнейшие направления развития включают:

- Интеграцию с OCR для полного извлечения метаданных из штампа
- Обучение на двух классах: "основная надпись" и "дополнительные элементы штампа"
- Расширение на другие формы штампов ГОСТ (FORM_1, FORM_2, FORM_6)
- Оценку на более крупной тестовой выборке (100+ изображений)
- Сравнение с другими архитектурами (Faster R-CNN, SSD)

Результаты исследования подтверждают перспективность использования методов компьютерного зрения для автоматизации обработки технической документации и могут быть использованы при проектировании систем цифровизации архивов проектных организаций.

---

## Список литературы

1. ГОСТ 2.104-2006. Единая система конструкторской документации. Основные надписи. — М. : Стандартинформ, 2006. — 24 с.

2. Чертежи, проекты и 3D модели 2d-3d.ru. https://www.2d-3d.ru/

3. File Converter — DWG to PNG. https://file-converter.org/dwg-to-png

4. LabelImg — Image Annotation Tool. GitHub. https://github.com/tzutalin/labelImg

5. Pillow — Python Imaging Library. https://python-pillow.org/

6. Jocher, G., et al. ultralytics/yolov8. GitHub, 2023. https://github.com/ultralytics/ultralytics

7. OpenCV (Open Source Computer Vision Library). https://opencv.org/

8. ГОСТ Р 7.0.100–2018. Библиографическая запись. Библиографическое описание. Общие требования и правила оформления.

---

## Приложение

- Репозиторий проекта: https://github.com/Sapar-hub/gost-ocr
- Визуальные примеры сравнения (Рис. 1–49): `output/evaluation/visualizations/`
- Датасет для обучения: `src/gost_ocr/datasets/yolo/images/train/`
- Тестовые данные: `src/gost_ocr/datasets/yolo/images/test/`
- Результаты эксперимента: `output/evaluation/results.json`
- Обученная модель: `src/gost_ocr/models/yolo/best.pt`

---

*Статья подготовлена в рамках курсовой работы по автоматизации обработки технической документации.*
